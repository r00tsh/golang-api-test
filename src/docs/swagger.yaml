info:
  contact: {}
paths:
  /health:
    get:
      consumes:
      - application/json
      description: do health
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: health
      tags:
      - health
swagger: "2.0"
